import { NativeModules } from 'react-native';

type BharatxReactnativeAlternatedataType = {
  register(): void;
};

const { BharatxReactnativeAlternatedata } = NativeModules;

export default BharatxReactnativeAlternatedata as BharatxReactnativeAlternatedataType;
